# SDDS Bunch Distribution Viewer
# Requirements for both v1 (CustomTkinter) and v2 (PySide6)
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Note for v2 (PySide6): requires Python >= 3.9
# Note: PySide6 and customtkinter are mutually optional depending on which version you run

# ── Shared (both versions) ────────────────────────────────────────────────────
numpy>=1.24.0
matplotlib>=3.7.0
scipy>=1.10.0

# ── v1 only (sddsbunchplot_v1.py) ────────────────────────────────────────────
customtkinter>=5.2.0

# ── v2 only (sddsbunchplot_v2.py) ────────────────────────────────────────────
PySide6>=6.8.0
